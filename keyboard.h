#pragma once

// keyboard keys. allows for WASD to be used as well as arrow keys when keys are [ w, s, d, a, ? ]
#define KEY_UP 'w'
#define KEY_DOWN 's'
#define KEY_RIGHT 'd'
#define KEY_LEFT 'a'
#define KEY_FAIL '?'

#include "keyboard.c"


